# Canon Exports

This directory is reserved for exported canonical snapshots written by the Vox OS GitHub export pipeline.

Expected shape:

- `canon/vox/latest.json`
- `canon/vox/v001.json`
- future versions as `v002.json`, `v003.json`, and so on

